machine:
  services:
    - docker
  node:
    version: 7.10.0

dependencies:
  cache_directories:
    - "/tmp/nexe"

test:
  override:
    - npm test
    - npm run nexe:package

deployment:
  production:
    tag: /[0-9]+(\.[0-9]+)*/
    commands:
      - docker build . -t intellizoom/edr:latest